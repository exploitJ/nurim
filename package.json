{
  "name": "nurim",
  "private": true,
  "license": "MIT",
  "engines": {
    "node": "^24",
    "pnpm": "^10"
  },
  "packageManager": "pnpm@10.31.0",
  "scripts": {
    "root:clean": "git clean -xdf .cache .turbo node_modules",
    "root:format": "prettier .github/ .vscode/ *.* --check --cache --cache-location .cache/prettiercache --cache-strategy metadata",
    "root:format:fix": "prettier .github/ .vscode/ *.* --write --cache --cache-location .cache/prettiercache --cache-strategy metadata",
    "build": "turbo run build",
    "build:desktop": "turbo run desktop#build:unpack",
    "release": "turbo run desktop#build:pack",
    "clean": "turbo run clean root:clean",
    "auth:generate": "pnpm -F @acme/auth generate",
    "db:push": "turbo -F @acme/db push",
    "db:studio": "turbo -F @acme/db studio",
    "dev": "turbo watch dev --continue",
    "dev:next": "turbo watch dev -F @acme/nextjs...",
    "ci:check": "turbo run format lint",
    "preformat": "pnpx prettierignore-monorepo@latest",
    "format": "turbo run format root:format --continue",
    "format:fix": "turbo run format:fix root:format:fix --continue",
    "lint": "turbo run lint --continue",
    "lint:fix": "turbo run lint:fix --continue",
    "quality": "turbo run quality --continue",
    "quality:fix": "turbo run quality:fix --continue",
    "lint:ws": "pnpx sherif@latest",
    "postinstall": "pnpm lint:ws --fix --select highest",
    "typecheck": "turbo run typecheck",
    "ui-add": "turbo run ui-add",
    "android": "expo run:android",
    "ios": "expo run:ios",
    "knip": "knip"
  },
  "devDependencies": {
    "@acme/prettier-config": "workspace:*",
    "@turbo/gen": "^2.8.14",
    "@types/node": "catalog:",
    "dotenv-cli": "^11.0.0",
    "knip": "^5.82.0",
    "prettier": "catalog:",
    "turbo": "^2.8.14",
    "typescript": "catalog:"
  },
  "prettier": "@acme/prettier-config"
}
